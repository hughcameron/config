# Managed via private git repo (hughcameron/claude)
.claude/

# Backlog.md project data (lives in repo, not deployed to home)
backlog/

{{ if ne .chezmoi.os "darwin" }}
# macOS-only configs - skip on Linux VMs
.config/ghostty/
.config/hammerspoon/
.config/zed/
.config/nushell/
.config/utm/
Library/
{{ end }}
