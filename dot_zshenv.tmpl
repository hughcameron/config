# ~/.zshenv - Loaded for ALL zsh sessions (interactive, non-interactive, scripts, SSH)
# Keep this minimal: only PATH and essential env vars

for file in \
    "$HOME/.config/zsh/env.sh" \
    "$HOME/.config/zsh/path.sh"; do
    [[ -f "$file" ]] && source "$file"
done
unset file
